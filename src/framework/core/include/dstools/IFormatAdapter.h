#pragma once
/// @file IFormatAdapter.h
/// @brief Backward compatibility shim. Use <dsfw/IFormatAdapter.h> instead.

#include <dsfw/IFormatAdapter.h>

namespace dstools {
    using dsfw::IFormatAdapter;
}