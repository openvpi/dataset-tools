#pragma once
/// @file ServiceLocator.h
/// @brief Backward compatibility shim. Use <dsfw/ServiceLocator.h> instead.

#include <dsfw/ServiceLocator.h>

namespace dstools {
    using dsfw::ServiceLocator;
}